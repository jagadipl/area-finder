// find out the length of word.

 text = "pragya"
 length = text.length
 console.log(`total length of ${text} is ${length}`)
  
 text = "prajiya lamsal"
 length = text.length
 console.log(`total length of ${text} is ${length}`)
  
 text = "jhapa"
 length = text.length
 console.log(`total length of ${text} is ${length}`)