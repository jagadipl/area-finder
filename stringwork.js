date = "Jagadip"
length = date.length
text = date.substring(2,4) // we can use slice also instead of substring.
console.log(text);
console.log(`total length of ${date} is ${length}`)
// this is for finding length of text or word.