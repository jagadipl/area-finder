fruits = ["mango","orange","banana","apple"]

// length find total number of items in list
total =  fruits.length
// pop() remove the last element from array
fruits.pop()

// push(): Insert New Items to array
fruits.push("ABC")
console.log(`Total fruits is ${total}`);

for(i=0; i<fruits.length; i++){
    console.log(fruits[i])
}

// Create array students add 3 students. Use push pop and find the length