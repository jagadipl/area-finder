// Create fruits array add 4 fruits to it and print details using loop.

fruits = ["mango","orange","banana","apple", "Grapes"]
fruits.pop()// pop removes the last item from array.
for(i=0; i<fruits.length; i++){
    console.log(fruits[i])
}
length = fruits.length
console.log(`total number of fruits ${length}`)
 