// find out the length of word.

 text = "pragya"
 length = text.length
 console.log(`total length of ${text} is ${length}`)
  
 text = "prajiyalamsal"
 length = text.length
 console.log(`total length of ${text} is ${length}`)
  
 text = "jhapa"
 length = text.length
 console.log(`total length of ${text} is ${length}`)

 text = "jagadip lamsal"
 length = text.length
 console.log(`total length of ${text} is ${length}`)

 text = "birtamode"
 length = text.length
 console.log(`total length ${text} is ${length}`)